# 📦 Метод MODI (UV-метод) 
## 🎯 Что такое транспортная задача?

Представьте, что у вас есть несколько складов с товарами и несколько магазинов, которым нужны эти товары. Нужно доставить товары так, чтобы **минимизировать общую стоимость** перевозок.

### Простой пример из жизни:
- **3 склада** (поставщики): у каждого есть определенное количество товара
- **4 магазина** (потребители): каждому нужно определенное количество товара
- **Стоимость доставки**: от каждого склада до каждого магазина своя цена

**Задача:** Как распределить товары, чтобы потратить минимум денег на доставку?

---

## 📊 Исходные данные

### 1. Предложение (Supply)
Сколько товара есть на каждом складе:
```
Склад 1: 20 единиц
Склад 2: 30 единиц
Склад 3: 25 единиц
Всего: 75 единиц
```

### 2. Спрос (Demand)
Сколько товара нужно каждому магазину:
```
Магазин 1: 15 единиц
Магазин 2: 25 единиц
Магазин 3: 20 единиц
Магазин 4: 15 единиц
Всего: 75 единиц
```

### 3. Матрица стоимостей (Cost Matrix)
Стоимость доставки 1 единицы товара от склада i до магазина j:

|         | Магазин 1 | Магазин 2 | Магазин 3 | Магазин 4 |
|---------|-----------|-----------|-----------|-----------|
| Склад 1 |     2     |     3     |    11     |     7     |
| Склад 2 |     1     |     0     |     6     |     1     |
| Склад 3 |     5     |     8     |    15     |     9     |

**Важно:** Сумма предложения = Сумма спроса (75 = 75). Это называется **сбалансированная задача**.

---

## 🔄 Алгоритм метода MODI - Пошагово

### ШАГ 1: Найти начальное решение

Сначала нам нужно **любое допустимое решение**. Мы используем **метод минимальной стоимости**:

1. Находим самую дешевую клетку в таблице
2. Отправляем туда максимально возможное количество товара
3. Вычеркиваем заполненный ряд или столбец
4. Повторяем, пока не распределим все товары

**Результат:** Матрица allocation (распределение), где allocation[i][j] = количество товара от склада i к магазину j.

#### Что такое базисные клетки?
- **Базисная клетка** = клетка, где allocation[i][j] > 0 (есть поставка)
- **Небазисная клетка** = клетка, где allocation[i][j] = 0 (нет поставки)

---

### ШАГ 2: Вычислить потенциалы u и v (UV-метод)

Это самая важная часть метода MODI! 

#### 🤔 Зачем нужны потенциалы?

Потенциалы u и v помогают нам понять, **можно ли улучшить решение**, не проверяя все возможные варианты.

#### 📝 Как вычислять u и v?

Для каждой **базисной** клетки должно выполняться уравнение:

```
u[i] + v[j] = cost[i][j]
```

Где:
- `u[i]` — потенциал поставщика (склада) i
- `v[j]` — потенциал потребителя (магазина) j
- `cost[i][j]` — стоимость перевозки

#### Алгоритм вычисления (волновой метод):

1. **Задаем начальное значение:** u[0] = 0 (произвольно выбираем первый потенциал)

2. **Распространяем значения волной:**
   - Смотрим на все базисные клетки
   - Если u[i] известно, а v[j] нет → вычисляем: **v[j] = cost[i][j] - u[i]**
   - Если v[j] известно, а u[i] нет → вычисляем: **u[i] = cost[i][j] - v[j]**

3. **Повторяем**, пока не узнаем все u и v

#### Пример:
```
Начинаем: u[0] = 0

Если allocation[0][1] > 0 (базисная клетка) и cost[0][1] = 3:
  u[0] + v[1] = 3
  0 + v[1] = 3
  v[1] = 3

Теперь, если allocation[2][1] > 0 и cost[2][1] = 8:
  u[2] + v[1] = 8
  u[2] + 3 = 8
  u[2] = 5

И так далее...
```

---

### ШАГ 3: Вычислить оценки w

Для **всех клеток** (базисных и небазисных) вычисляем:

```
w[i][j] = cost[i][j] - u[i] - v[j]
```

#### 🎯 Что означает w?

- **w[i][j] >= 0** → использование этой клетки **не улучшит** решение
- **w[i][j] < 0** → использование этой клетки **улучшит** решение (уменьшит стоимость)

**Важно:** Для базисных клеток w всегда = 0 (по определению u + v = cost).

---

### ШАГ 4: Проверить оптимальность

Смотрим на **небазисные клетки**:

- ✅ **Если все w >= 0** → решение **ОПТИМАЛЬНО!** Останавливаемся.
- ❌ **Если есть w < 0** → решение можно **УЛУЧШИТЬ!** Переходим к шагу 5.

---

### ШАГ 5: Улучшить решение

Если решение не оптимально, делаем улучшение:

#### 5.1. Выбрать входящую клетку
Находим небазисную клетку с **самым отрицательным** w (самым большим улучшением).

#### 5.2. Построить цикл
Строим **замкнутый цикл** из горизонтальных и вертикальных линий:
- Начинаем и заканчиваем в выбранной небазисной клетке
- Проходим только через **базисные клетки**
- Двигаемся только по горизонтали или вертикали

**Визуально:**
```
○ — небазисная клетка (входящая)
● — базисные клетки
→ ↑ ← ↓ — линии цикла

     Маг1  Маг2  Маг3  Маг4
Скл1        ●───→○
            ↑    ↓
Скл2   ●    ●←───●
       ↑
Скл3   ●
```

#### 5.3. Перераспределить поставки
По циклу чередуются знаки: **+ - + - + - ...**

1. Находим **минимальное значение** в клетках со знаком **минус**
2. Прибавляем это значение к клеткам со знаком **плюс**
3. Вычитаем из клеток со знаком **минус**

**Результат:** Одна базисная клетка станет небазисной (уйдет из базиса), а наша выбранная клетка станет базисной.

---

### ШАГ 6: Повторить

Возвращаемся к **Шагу 2** и повторяем процесс, пока не получим оптимальное решение.

---

## 💡 Почему это работает?

### Математическая интуиция:

1. **u и v** — это "потенциалы" или "теневые цены"
   - u[i] показывает, насколько "ценен" товар на складе i
   - v[j] показывает, насколько "ценна" доставка в магазин j

2. **w[i][j]** — это "приведенная стоимость"
   - Показывает, выгодно ли использовать маршрут i→j
   - Если w < 0, то маршрут выгоден (сэкономим деньги)

3. **Цикл перераспределения**
   - Позволяет изменить решение, не нарушая баланс
   - Гарантирует, что предложение и спрос остаются удовлетворенными

---

## 🖥️ Как использовать программу?

### Базовое использование:

```python
import numpy as np
from zhaniya_is_legend import modi_method

# Ваши данные
supply = [20, 30, 25]  # Запасы складов
demand = [15, 25, 20, 15]  # Потребности магазинов

cost = np.array([
    [2, 3, 11, 7],
    [1, 0, 6, 1],
    [5, 8, 15, 9]
])

# Решение задачи
allocation, total_cost = modi_method(supply, demand, cost)

print("Оптимальное распределение:")
print(allocation)
print(f"Минимальная стоимость: {total_cost}")
```

### Результат программы:

```
Оптимальное распределение:
[[  0.  20.   0.   0.]
 [ 15.   5.  10.   0.]
 [  0.   0.  10.  15.]]

Минимальная стоимость: 115.0
```

**Интерпретация:**
- Склад 1 → Магазин 2: 20 единиц
- Склад 2 → Магазин 1: 15 единиц
- Склад 2 → Магазин 2: 5 единиц
- Склад 2 → Магазин 3: 10 единиц
- Склад 3 → Магазин 3: 10 единиц
- Склад 3 → Магазин 4: 15 единиц

**Общая стоимость:** 115 денежных единиц

---

## ⚠️ Важные ограничения

### 1. Сбалансированность
Сумма предложения **ДОЛЖНА** равняться сумме спроса!

```python
sum(supply) == sum(demand)  # Обязательно!
```

Если не равны, нужно добавить **фиктивный** склад или магазин с нулевой стоимостью.

### 2. Количество базисных клеток
Правильное количество базисных клеток = m + n - 1

Где:
- m = количество поставщиков
- n = количество потребителей

В нашем примере: 3 + 4 - 1 = 6 базисных клеток.

### 3. Невырожденность
Если базисных клеток меньше, чем m + n - 1, задача **вырожденная**. Нужно добавить фиктивные поставки (очень малые числа, например, 0.0001).

---

## 🔍 Отладка и проверка

### Как проверить, что решение правильное?

1. **Проверка баланса поставщиков:**
```python
for i in range(len(supply)):
    row_sum = sum(allocation[i])
    print(f"Склад {i+1}: {row_sum} == {supply[i]}")
```

2. **Проверка баланса потребителей:**
```python
for j in range(len(demand)):
    col_sum = sum(allocation[i][j] for i in range(len(supply)))
    print(f"Магазин {j+1}: {col_sum} == {demand[j]}")
```

3. **Проверка оптимальности:**
```python
u, v = get_u_and_v(cost, allocation)
w = get_w(cost, allocation, u, v)

# Для небазисных клеток все w должны быть >= 0
for i in range(m):
    for j in range(n):
        if allocation[i][j] == 0 and w[i][j] < 0:
            print(f"Ошибка! w[{i}][{j}] = {w[i][j]} < 0")
```

---

## 📚 Термины и определения

| Термин | Определение |
|--------|-------------|
| **Поставщик** | Склад или источник товара |
| **Потребитель** | Магазин или пункт назначения |
| **Предложение (Supply)** | Количество товара на складе |
| **Спрос (Demand)** | Потребность магазина в товаре |
| **Стоимость (Cost)** | Цена доставки 1 единицы товара |
| **Распределение (Allocation)** | Сколько товара везем от каждого склада в каждый магазин |
| **Базисная клетка** | Маршрут, который используется (allocation > 0) |
| **Небазисная клетка** | Маршрут, который не используется (allocation = 0) |
| **Потенциал u[i]** | Характеристика поставщика i |
| **Потенциал v[j]** | Характеристика потребителя j |
| **Оценка w[i][j]** | Показатель выгодности маршрута i→j |
| **Цикл** | Замкнутый путь для перераспределения товаров |

---

## 🎓 Советы для начинающих

### 1. Начните с простого примера
Попробуйте задачу 2×2 или 2×3 сначала, чтобы понять принцип.

### 2. Рисуйте таблицы
Визуализация помогает! Рисуйте матрицу и отмечайте базисные клетки.

### 3. Проверяйте каждый шаг
После каждой итерации проверяйте:
- Баланс поставщиков и потребителей
- Количество базисных клеток
- Значения u, v и w

### 4. Используйте калькулятор
Не стесняйтесь использовать калькулятор для проверки расчетов вручную.

### 5. Понимайте логику
Не просто запускайте программу — поймите, **почему** каждый шаг делается именно так.

---

## 🚀 Расширенные возможности

### Несбалансированная задача
Если sum(supply) ≠ sum(demand), добавьте:
- **Фиктивный склад** (если спрос > предложение)
- **Фиктивный магазин** (если предложение > спрос)

С нулевыми стоимостями доставки.

### Максимизация
Если нужно **максимизировать** прибыль вместо минимизации стоимости:
1. Умножьте все значения прибыли на -1
2. Решите задачу минимизации
3. Умножьте результат на -1

### Запрещенные маршруты
Если какой-то маршрут невозможен (например, дороги нет), установите:
```python
cost[i][j] = float('inf')  # Бесконечная стоимость
```

---

## ❓ Частые вопросы

### Q: Почему u[0] = 0?
**A:** Это произвольный выбор. Можно задать любое u[i] = 0, результат будет тот же. Главное — зафиксировать одно значение для начала.

### Q: Что если не удается найти цикл?
**A:** Это означает ошибку в данных или в алгоритме. Проверьте:
- Количество базисных клеток = m + n - 1
- Нет ли вырожденности
- Правильно ли помечены базисные клетки

### Q: Сколько итераций нужно?
**A:** Обычно 3-10 итераций для задач средней сложности. Программа ограничена 1000 итераций на всякий случай.

### Q: Можно ли решить задачу 100×100?
**A:** Да! Метод MODI работает для любых размеров, но большие задачи могут требовать больше времени.

---

## 📖 Дополнительные ресурсы

### Альтернативные методы начального решения:
1. **Метод северо-западного угла** (самый простой)
2. **Метод минимальной стоимости** (используется в программе)
3. **Метод Фогеля (VAM)** (даёт лучшее начальное решение)

### Связанные темы:
- Симплекс-метод (общий метод линейного программирования)
- Венгерский алгоритм (задача о назначениях)
- Задача коммивояжёра (TSP)

---

## 🎉 Заключение

Метод MODI — это мощный и элегантный способ решения транспортных задач. Главное:

1. ✅ Понимать логику потенциалов u и v
2. ✅ Уметь строить циклы перераспределения
3. ✅ Проверять оптимальность через оценки w
4. ✅ Повторять итерации до оптимума

**Удачи в решении ваших транспортных задач!** 🚚📦

---

## 📝 Авторы и лицензия

Создано для образовательных целей.  
Свободное использование для обучения и исследований.

**Дата:** 2025  
**Версия:** 1.0

