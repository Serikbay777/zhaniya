# 📋 Метод MODI - Краткая шпаргалка

## 🎯 Основная формула

Для базисных клеток:
```
u[i] + v[j] = cost[i][j]
```

Для всех клеток:
```
w[i][j] = cost[i][j] - u[i] - v[j]
```

---

## 📊 Алгоритм за 6 шагов

### 1️⃣ Начальное решение
Методом минимальной стоимости или северо-западного угла

### 2️⃣ Вычислить u и v
- Задать u[0] = 0
- Для базисных клеток:
  - Если известно u[i] → **v[j] = cost[i][j] - u[i]**
  - Если известно v[j] → **u[i] = cost[i][j] - v[j]**
- Повторять волной, пока все не найдены

### 3️⃣ Вычислить w
Для всех клеток: **w[i][j] = cost[i][j] - u[i] - v[j]**

### 4️⃣ Проверить оптимальность
- **Все w >= 0 для небазисных?** → ✅ ОПТИМУМ! Стоп.
- **Есть w < 0?** → ❌ Не оптимум, продолжить

### 5️⃣ Улучшить решение
- Выбрать небазисную клетку с **min(w)** (самое отрицательное)
- Построить замкнутый цикл через базисные клетки
- Найти min значение в клетках со знаком "−"
- Перераспределить: добавить к "+", вычесть из "−"

### 6️⃣ Повторить
Вернуться к шагу 2

---

## ⚡ Быстрые проверки

### ✅ Сбалансированность
```
sum(supply) == sum(demand)
```

### ✅ Количество базисных клеток
```
m + n - 1
```
где m = поставщики, n = потребители

### ✅ Баланс поставщика i
```
sum(allocation[i, :]) == supply[i]
```

### ✅ Баланс потребителя j
```
sum(allocation[:, j]) == demand[j]
```

---

## 🔑 Ключевые термины

| Символ | Значение |
|--------|----------|
| **m** | Количество поставщиков |
| **n** | Количество потребителей |
| **u[i]** | Потенциал поставщика i |
| **v[j]** | Потенциал потребителя j |
| **w[i][j]** | Оценка клетки (приведенная стоимость) |
| **cost[i][j]** | Стоимость перевозки от i к j |
| **allocation[i][j]** | Количество товара от i к j |

---

## 💻 Код - Шаблон использования

```python
import numpy as np
from zhaniya_is_legend import modi_method

# Данные
supply = [20, 30, 25]
demand = [15, 25, 20, 15]
cost = np.array([
    [2, 3, 11, 7],
    [1, 0, 6, 1],
    [5, 8, 15, 9]
])

# Решение
allocation, total_cost = modi_method(supply, demand, cost)

print("Распределение:", allocation)
print("Стоимость:", total_cost)
```

---

## ⚠️ Частые ошибки

❌ **Ошибка:** Применять формулы u+v=c к небазисным клеткам  
✅ **Правильно:** Только для базисных (allocation > 0)

❌ **Ошибка:** w должны быть >= 0 для всех клеток  
✅ **Правильно:** Только для небазисных клеток

❌ **Ошибка:** Цикл через небазисные клетки  
✅ **Правильно:** Цикл только через базисные (кроме стартовой)

❌ **Ошибка:** sum(supply) ≠ sum(demand)  
✅ **Правильно:** Добавить фиктивный ряд/столбец

---

## 🎓 Критерий оптимальности

```
Решение ОПТИМАЛЬНО ⟺ 
для всех небазисных клеток (i,j): w[i][j] >= 0
```

**Почему?**  
w < 0 означает, что использование маршрута i→j **уменьшит** общую стоимость.

---

## 🔄 Цикл перераспределения

```
Шаг 1: Найти входящую клетку (небазисную с min w)
Шаг 2: Построить цикл:
       ○ → ● → ● → ● → ○
       ↑           ↓
       ●←───●←───●
       
Шаг 3: Расставить знаки: + - + - + - ...
Шаг 4: θ = min(allocation в клетках с "−")
Шаг 5: Прибавить θ к "+", вычесть θ из "−"
```

---

## 📈 Сложность алгоритма

- **Время:** O(m × n × iterations)
- **Пространство:** O(m × n)
- **Итерации:** Обычно 3-10, максимум ~(m+n)²

---

## 🛠️ Отладка

### Не находятся все u и v?
→ Проверьте количество базисных клеток (должно быть m+n-1)

### Не находится цикл?
→ Задача вырожденная, добавьте ε (0.0001) в пустую клетку

### Бесконечный цикл?
→ Проверьте, что базисные клетки корректно обновляются

### Отрицательная стоимость?
→ Проверьте входные данные и знаки в цикле перераспределения

---

## 🚀 Быстрый старт

```bash
# Запуск программы
python3 zhaniya_is_legend.py

# Вывод: оптимальное решение и стоимость
```

---

**Автор:** Educational Project  
**Дата:** 2025  
**Полное руководство:** `MODI_METHOD_GUIDE.md`

